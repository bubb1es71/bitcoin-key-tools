[workspace]
resolver = "3"
members = ["seedroller","keyderiver"]

[workspace.package]
description = "Tools to generate a bitcoin seed and derive keys from it."
edition = "2024"
license = "MIT OR Apache-2.0"

[profile.release]
strip = true
